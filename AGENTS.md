# AI Agent Instructions

## Project overview
- Small C++ practice project in `testing-c-`
- Primary source is `main.cpp`
- `index.cpp` is present but currently empty
- No build system or tests are provided in the repository

## Build and run
- Use a standard C++ compiler to build the executable
- Example command:
  - `g++ main.cpp -o main`
  - or `g++ *.cpp -o main`
- Run the program with `./main` on Linux/macOS or `main.exe` on Windows

## Coding conventions
- Code is simple and self-contained in `main.cpp`
- Comments and variable names may use Spanish
- Keep changes minimal and aligned with beginner C++ style unless the user asks for refactoring

## Guidance for AI agents
- Do not assume a test suite exists unless the user adds one
- If asked to improve the project, prefer adding a simple build file or README instructions rather than changing the repository layout drastically
- If code behavior is unclear, ask the user for the intended exercise or output
